# Claude용 진입점

이 저장소의 공통 작업 지침은 [AGENTS.md](AGENTS.md)에 있다. 작업 시작 시 해당 파일과 작업 범위에 맞는 상세 문서를 읽고 따른다.

공통 규칙은 `docs/agents/`에서 관리한다. `.claude/`의 기존 규칙은 이전 버전이며, 차이가 있으면 `AGENTS.md`와 새 상세 문서를 기준으로 한다. `.claude/settings.json`과 `.claude/hooks/`는 Claude 전용 설정으로 유지한다. 기존 `/work` 명령을 사용할 때도 [공통 작업 절차](docs/agents/work.md)를 따른다.
